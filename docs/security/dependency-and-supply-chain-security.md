# Dependency and Supply-Chain Security

Use lockfiles, minimal dependencies, pinned GitHub Actions, least-privilege workflow permissions, secret scanning, dependency review, SAST, container scanning, SBOM generation and provenance or signing where practical.

Do not add packages for trivial functionality without justification. Authentication, cryptography, networking and persistence dependency changes require explicit review.
