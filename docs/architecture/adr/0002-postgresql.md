# ADR 0002: PostgreSQL

**Status:** Accepted

PostgreSQL is the authoritative datastore because the product is relational, multi-tenant and transaction-heavy. SQLite is excluded from application environments.
