# ADR 0004: Caddy Reverse Proxy

**Status:** Accepted

Caddy is the initial origin reverse proxy for simple routing, TLS, compression and security-header control. Only Caddy binds public application ports.
