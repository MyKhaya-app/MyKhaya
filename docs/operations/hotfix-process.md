# Urgent Fixes

Urgent fixes follow the same simple workflow as every other change: implement and validate the smallest safe fix on `dev`, then report that `dev` is ready.

Anthony decides whether and when to merge `dev` into `main`, tag and deploy. Do not create a `hotfix/*` branch unless Anthony explicitly requests one. Never automatically merge a fix into either permanent branch.
